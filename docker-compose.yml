version: "3.7"
services:
  wandb:
    image: wandb/local
    container_name: wandb
    ports:
      - "8080:8080"
    volumes:
      - ./jwks.json:/var/app/jwks.json:ro
    environment:
      - HOST=http://192.168.18.170:8080
